{
  "name": "gofakeit-extension",
  "version": "1.0.0",
  "description": "Chrome extension to autofill inputs using gofakeit-style data",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.0.0"
  }
}
